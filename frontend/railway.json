{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm --prefix .. install",
    "watchPatterns": ["../backend/**", "../package.json", "../package-lock.json", "railway.json"]
  },
  "deploy": {
    "startCommand": "npm --prefix .. start"
  }
}
