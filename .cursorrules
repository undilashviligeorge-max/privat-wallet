1. NEVER wait for the user to click 'Accept', 'Review', or 'Commit' in the UI.
2. ALWAYS forcefully save changes via terminal using `git add . && git commit -m 'auto-update'`.
3. ALWAYS execute necessary terminal commands autonomously.
